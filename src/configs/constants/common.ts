export const MAX_PASSWORD_VALUE = 6;
export const TOKEN_EXPIRES = 60;

export enum Providers {
  GOOGLE = 'google',
  GITHUB = 'github',
  CREDENTIALS = 'credentials',
}

export enum UserRoles {
  USER = 'USER',
  COMPANY = 'COMPANY',
}
